spring:
  application:
    name: componente-b
  datasource:
    url: jdbc:postgresql://localhost:5432/proveedores_facturacion_db
    username: usuario
    password: contraseña
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: America/Guatemala

server:
  port: 8081

logging:
  level:
    com.multipedidos: DEBUG
    org.hibernate.SQL: DEBUG